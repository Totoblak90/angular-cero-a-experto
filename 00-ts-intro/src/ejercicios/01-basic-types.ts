

let nombre: string = 'Strider';
let hp: number | string = 95;
let estaVivo: boolean | string = true;

if (estaVivo) {
    estaVivo = 'Vive'
} else {
    estaVivo = 'Muerto'
}

hp = 'Full';

console.log("EL personaje se llama: " + nombre + ". Barra de vida: " + hp + " Estado: " + estaVivo);

/*
    ===== Código de TypeScript =====
*/

